pipeline {
    agent any

    stages {
        stage('Check_binaries') {
            steps {
                sh "python --version"
            }
        }

        // stage('Execute_progrm') {
        //     steps {
        //         python3 test.py
        //     }
        // }
    }
}